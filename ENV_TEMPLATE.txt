# Swarm Relay - Environment Variables Template
# Copy these to your Zeabur environment settings

# === DEVELOPMENT ===
DOCKER_ENV=false

# === REQUIRED ===
RELAY_NAME="Swarm Relay"
RELAY_PUBKEY="d7416884c7c468ad9e5fd1f2538c7f893318e074998e3538bc827155ef598706"
RELAY_DESCRIPTION="Team Nostr relay"

# === SERVER ===
RELAY_PORT=3334
WEBSOCKET_URL=wss://localhost:3334

# === DATABASE ===
DB_ENGINE=postgres
# DB_ENGINE=badger # lmdb, badger, postgres (default: postgres)
# DB_PATH=db/ # only required for badger and lmdb

# PostgreSQL (takes precedence if set)
DATABASE_URL=postgres://swarm:password@localhost:5437/relay?sslmode=disable

# Individual PostgreSQL variables (used if DATABASE_URL is not set)
# POSTGRES_USER=swarm
# POSTGRES_PASSWORD=password
# POSTGRES_DB=relay
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5437

# === TEAM / DOMAIN ===
TEAM_DOMAIN="swarm.hivetalk.org"
NPUB_DOMAIN="hivetalk.org"

# === BLOSSOM MEDIA STORAGE ===
BLOSSOM_ENABLED=false
BLOSSOM_PATH=blossom/
BLOSSOM_URL=http://localhost:3334
MAX_UPLOAD_SIZE_MB=200
ALLOWED_MIRROR_HOSTS=

# === S3/TIGRIS STORAGE (optional) ===
STORAGE_BACKEND=filesystem
# STORAGE_BACKEND=s3
S3_ENDPOINT=https://t3.storage.dev
S3_BUCKET=swarm
S3_REGION=auto
AWS_ACCESS_KEY_ID=tid_xxxxx
AWS_SECRET_ACCESS_KEY=tsec_xxxxx
S3_PUBLIC_URL=

# === NIP-05 SERVICE ===
NIP05_PATH=public/.well-known/nostr.json

# === EVENT KIND FILTERING ===
ALLOWED_KINDS=""
PUBLIC_ALLOWED_KINDS=""

# === SPAM RATE LIMITING (optional) ===
# If unset or empty, each limiter is disabled
# PUBKEY_RATE_LIMIT=50
# IP_RATE_LIMIT=100
# CONN_RATE_LIMIT=20
# QUERY_RATE_LIMIT=300

# === TRUSTED CLIENT ===
TRUSTED_CLIENT_NAME=""
TRUSTED_CLIENT_KINDS=""
